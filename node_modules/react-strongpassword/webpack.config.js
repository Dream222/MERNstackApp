var path = require('path');

module.exports = {
    entry: './src/strongPassword.jsx',
    output: {
        path: path.join(__dirname, 'lib'),
        filename: 'react-strongpassword.js',
        libraryTarget: 'umd',
        umdNamedDefine: true
    },
    externals: [
        "react",
        "zxcvbn",
        "lodash.debounce"
    ],
    target: "web",
    module: {
        loaders: [
            {test: /\.(js|jsx)$/, loader: "babel-loader", exclude: /node_modules/},
            {test: /\.(jpg|gif|png|svg)$/, loader: "file-loader?name=img/[hash].[ext]"},
            {test: /\.(scss|css)$/, loaders: ['style', 'css', 'sass']}
        ]
    },
    resolveLoader: {root: path.join(__dirname, 'node_modules')}
};
